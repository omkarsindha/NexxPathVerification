// DO NOT change any labels in this file, you can add more commands between "LABEL" and "END_LABEL"
// "//" represents a comment
// Alias commands wont work so you have write the full command like the one below for "up". Find the full command using "type your_cmd"

CMDS
cd /evertz/test/nexx-xc
chmod +x routeall.sh
./routeall.sh
END_CMDS

IOS_CMDS
regif fs.ifce.fs_tg_en[0:7]=0xaa
END_IOS_CMDS